import styled, { th, css, down } from '@xstyled/styled-components';

const SPreviewMode = styled.divBox`
  width: 100%;
  margin: 0 auto;
  position: fixed;
  top: 0;
  left: 0;

  background-color: light;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
`;

export default SPreviewMode;
